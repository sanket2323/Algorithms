package Circularll;

public class CustomCll {
    public static void main(String[] args) {
        Cll cll=new Cll();
        //Inserting the element in the circular linked list
        cll.insert(233);
        cll.insert(23);
        cll.insert(43);
        cll.insert(3);
        //Display the circular linked list
        cll.display();
        //Deleting the a required item in the ciruclar linked list

        
    }
}
